name: offline_first_chat_bloc_mvvm

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  connectivity_plus: ^6.0.3
  cloud_firestore: ^5.4.0
  firebase_core: ^3.6.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0

flutter:
  uses-material-design: true
